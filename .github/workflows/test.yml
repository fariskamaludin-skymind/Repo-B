name: Create PR

on:
  push:
    branches: test-b

jobs:
  PR:
    name: Create PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repos
        uses: actions/checkout@v2

      - name: Create PR request
        uses: repo-sync/pull-request@v2
        with:
          github_token: ${{ secrets.PERSONAL_TOKEN }}
          destination_branch: "main"                      # If blank, default: master
          pr_title: "Pulling ${{ github.ref }} into main" # Title of pull request
          pr_body: ":crown: *An automated PR*"              # Full markdown support, requires pr_title to be set
          #pr_template: ".github/PULL_REQUEST_TEMPLATE.md"   # Path to pull request template, requires pr_title to be set, excludes pr_body
          pr_reviewer: "fariskamaludin-skymind"                         # Comma-separated list (no spaces)
          pr_assignee: "fariskamaludin-skymind"                         # Comma-separated list (no spaces)
          pr_label: "auto-pr"                               # Comma-separated list (no spaces)
          #pr_milestone: "Milestone 1"                       # Milestone name
          #pr_draft: true                                    # Creates pull request as draft
          pr_allow_empty: true                              # Creates pull request even if there are no changes